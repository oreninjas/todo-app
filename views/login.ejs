<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo App | Faiyan</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-sm bg-zinc-800 bg-opacity-80 p-8 rounded-xl shadow-lg backdrop-blur-md">
      <h3 class="text-3xl font-semibold text-center text-white mb-6">Login</h3>
      <form action="/login" method="post">
        <!-- Inputs -->
        <input
          class="w-full px-4 py-3 rounded-md bg-transparent border border-zinc-600 text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition duration-300 mb-5"
          type="email"
          name="email"
          placeholder="Email"
          required
          autocomplete="off"
        />
        <input
          class="w-full px-4 py-3 rounded-md bg-transparent border border-zinc-600 text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition duration-300 mb-5"
          type="password"
          name="password"
          placeholder="Password"
          required
        />
        <!-- Register -->
        <p class="text-center text-sm text-zinc-400">
          Don't have an account?
          <a href="/register" class="text-blue-400 hover:text-blue-500 transition duration-300">Register</a>
        </p>
        <input
          class="w-full bg-blue-600 text-white py-3 rounded-md text-xl mt-5 cursor-pointer hover:bg-blue-700 transition duration-300"
          type="submit"
          value="Login"
        />
        <!-- Error Message -->
        <% if(msg){ %>
        <p class="msg text-red-500 mt-4 text-center font-medium"><%= msg ? msg[0] : null %></p>
        <% } %>
      </form>
    </div>

    <script>
      setTimeout(() => {
        const msgElement = document.querySelector(".msg");
        if (msgElement) {
          msgElement.style.display = "none";
        }
      }, 3000);
    </script>
  </body>
</html>
