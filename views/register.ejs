<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gradient-to-r from-blue-500 via-indigo-600 to-purple-700 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-md bg-zinc-800 bg-opacity-80 p-8 rounded-2xl shadow-lg backdrop-blur-md">
      <h3 class="text-3xl font-semibold text-center text-white mb-6">Create Account</h3>
      <form action="/create/user" method="post">
        <input
          class="w-full px-4 py-3 mb-4 rounded-lg bg-transparent border border-zinc-600 text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition duration-300"
          type="text"
          name="username"
          placeholder="Username"
          required
        />
        <input
          class="w-full px-4 py-3 mb-4 rounded-lg bg-transparent border border-zinc-600 text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition duration-300"
          type="email"
          name="email"
          placeholder="Email"
          required
          autocomplete="off"
        />
        <input
          class="w-full px-4 py-3 mb-6 rounded-lg bg-transparent border border-zinc-600 text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition duration-300"
          type="password"
          name="password"
          placeholder="Password"
          required
        />
        <!-- Login Link -->
        <p class="text-center text-sm text-zinc-400 mb-4">
          Already have an account?
          <a href="/login" class="text-blue-400 hover:text-blue-500 transition duration-300">Login</a>
        </p>
        <input
          class="w-full bg-blue-600 text-white py-3 rounded-lg text-xl cursor-pointer hover:bg-blue-700 transition duration-300"
          type="submit"
          value="Register"
        />
      </form>
    </div>
  </body>
</html>
